---
- hosts: default
  become: true
  tasks:
    - name: install useful tools
      package:
        name: "{{ item }}"
      with_items:
        - git
        - htop
        - vim

    - include: remote.yml
    - include: libvmi.yml
    - include: rekall.yml
    - include: test_vm.yml
